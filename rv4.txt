=== chatgpt-codex-connector persistence/syncMerge.ts:340 [08-29T02:42] ===
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Abort the push when handoff-ledger restoration fails** When any `loadKv`/`saveKv` operation in this block fails—for example because the DB write is rejected—the catch suppresses it and `settleLocalWrites()` can still succeed because `drainSaves()` masks earlier save-chain rejection. The subsequent push therefor

=== chatgpt-codex-connector persistence/syncMerge.ts:324 [08-29T02:42] ===
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Stop finalization when the selected mindtrain write fails** When the user selects the folder-side route map and `saveKv('mindtrain', ...)` fails, this catch continues to push and eventually clears dirty. The pushed DB still contains this PC's old route map, so the operation does the opposite of the selected res

=== chatgpt-codex-connector persistence/handoff.ts:478 [08-29T02:42] ===
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Persist the receive ledger without stranding the committed import** If this ledger write fails after `commitSync` has already added the received project, the catch in `doImport` only displays the error and leaves a project that has no receipt entry. After reload the project cannot be exported as a return, and r

=== chatgpt-codex-connector persistence/handoff.ts:555 [08-29T02:42] ===
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Revalidate the return target master at commit time** If the source/active master is deleted by an undo or shortcut while `computeReturnMerge` is awaiting base/media I/O, `targetMaster` remains the ID chosen from the stale state passed at preparation time. Remote-added items have an absent `expectedBefore`, so t

=== chatgpt-codex-connector persistence/handoff.ts:643 [08-29T02:42] ===
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Keep skipped remote deletions in the next merge base** When a remote deletion is skipped because the lender edited that item while the modal was open, `merged` still contains the item, but `nextBase` starts only from the returned pack, where the deleted item is absent. Thus the saved base also omits it; reopeni

=== chatgpt-codex-connector persistence/syncMerge.ts:353 [08-29T02:42] ===
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Upload local media before publishing the merged DB** In a conflict where this PC keeps a local item containing newly added media, this direct `api.push` publishes a manifest whose DB references blobs that may not yet exist in the shared folder. Normal pushes explicitly run `pushMissingMedia` before the DB commi

=== chatgpt-codex-connector components/SyncMergeModal.tsx:111 [08-29T02:42] ===
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Refresh mindtrain state after a later finalization failure** When the folder-side route map was selected, `finalizeSyncMerge` writes it before attempting the backup and push. If either later step fails, this early return leaves SQLite containing the selected remote route map while the in-memory mindtrain store 

=== chatgpt-codex-connector components/SyncMergeModal.tsx:112 [08-29T02:42] ===
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Report skipped merge rows before reloading for mindtrain** When the route map is taken and one or more AppState operations are skipped because their items changed while the modal was open, this unconditional reload runs before the later `result.skipped` check. The user therefore never sees that selected remote 

=== chatgpt-codex-connector persistence/handoff.ts:649 [08-29T02:42] ===
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Keep the return unresolved when base advancement fails** If saving the advanced `handoff.base.*` snapshot fails transiently, this catch suppresses the error and continues to mark the index entry as returned; the save chain can recover for that second write, so the UI reports success while the old base remains. 

=== chatgpt-codex-connector persistence/handoff.ts:102 [08-29T02:42] ===
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Do not treat receipt-ledger read failures as an empty ledger** When `loadKv` rejects transiently, this helper returns `[]`, which `prepareReceive` treats as an authoritative empty receipt ledger. The later successful `finalizeReceive` then writes only the new receipt and permanently removes every previous recei

=== chatgpt-codex-connector main/sync.ts:294 [08-29T02:42] ===
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Reject a prepared pull after the sync folder changes** If the user changes or disables the sync folder while `pullPrepare` is reading the old folder, `prepared` still contains that old folder's DB. `pullCommit` reloads the current settings but checks only that some folder is configured and that the generation m

=== coderabbitai persistence/handoff.ts:479 [08-29T02:43] ===
_️ Data Integrity & Integration_ | _ Major_ | _⚡ Quick win_ **受領台帳の保存失敗時に取り込み結果を確定しないでください。** 取り込み結果を状態へ適用した後、`handoff.recv.index` の保存に失敗しても状態が残ります。台帳に記録されないため、同じ作業ファイルを再度取り込むと重複したプロジェクトや項目が追加される可能性があります。台帳保存と取り込み確定を原子的に扱うか、保存失敗時に取り込みをロールバックし、同一ファイルの再取り込みで重複しない回帰テストを追加してください。

=== coderabbitai persistence/handoff.ts:649 [08-29T02:43] ===
_ Functional Correctness_ | _ Minor_ | _⚡ Quick win_ **返却台帳の更新結果を呼び出し側へ返してください。** 647-649行は `handoff.index` の `returnedAt` を更新します。この `saveKv` は try/catch の外にあります。失敗すると `finalizeReturnMerge` が throw します。 `HandoffModal.tsx` の142行と188行はこの関数を await します。throw すると catch がエラー文だけを表示します。マージは既にコミット済みなので、ユーザーには「返却を取り込みました(更新 N / 追加 N / 削除 N)」の結果が届きません。ユーザーはマージが適用されたかどうかを判断できません。

=== coderabbitai persistence/syncMerge.ts:325 [08-29T02:43] ===
_️ Data Integrity & Integration_ | _ Major_ | _⚡ Quick win_ **`finalizeSyncMerge` は push 前の永続化失敗を無視します。** 343-352行は `settleLocalWrites` と `backupConflict` の失敗で送信を中止します。しかし 324行と340行は失敗を catch して push へ進みます。どちらの経路でも、353行の `api.push` がフォルダ側 DB を上書きし、相手側にしか存在しないデータが消えます。 - `src/renderer/src/persistence/syncMerge.ts#L320-L325`: `saveKv('mindtrain', ...)` が失敗した場合は `{ ok: false, message: ... }` を返して中止して